<div class="btn-retour">
  <%= link_to realty_path(@realty), class: "btn-primary-claim" do %>
    <i class="fa-sharp fa-solid fa-arrow-left"></i>
  <% end %>
</div>
<div class="container_claim">
      <h5>Ma liste des documents</h5>
    <div class="document" >
    <div class="card">
    <div class="card-body">
      <ul>
        <% @documents.each do |document| %>
            <li >
              <%= document.name %>
              <% if document.photo.attached? %>
              <div class="icone-document">
                <%= link_to raw('<i class="fa-regular fa-eye"></i>'), url_for(document.photo), target: "_blank" %>
              </div>
              <% else %>
                <span class="text-muted">Aucun fichier disponible</span>
              <% end %>
              <% if document.photo.attached? %>
              <div class="icone-document">
              <%= link_to raw('<i class="fas fa-download"></i>'), rails_blob_url(document.photo, disposition: 'attachment') %>
              </div>
              <% end %>
              <div class="icone-document">
              <%= link_to raw('<i class="fa-regular fa-circle-xmark"></i>'), document_path(document), data: { turbo_method: :delete, turbo_confirm: "Etes-vous sûr de vouloir supprimer ce document?" } %>
              </div>
            </li>
          <% end %>
      </ul>
    </div>
    </div>
  </div>


  <div class="card">
    <div class="card-body">
      <h5>Ajouter un document</h5>
      <%= simple_form_for([@realty, @document]) do |f| %>
        <%= f.input :photo, as: :file %>
        <%= f.input :name %>
        <%= f.button :submit, "Ajouter le document", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
