<div class="container">
  <h1>Bienvenue sur la page d'accueil des biens immobiliers !</h1>
  <div class="row">
    <% if @location_realties.any? %>
      <h2>Vos locations</h2>
      <% @location_realties.each do |realty| %>
        <div class="col-md-4">
          <div class="card mb-4">
            <div class="card-body">
              <h5 class="card-name"><%= realty.name %></h5>
              <%= link_to "Votre bien", realty_path(realty), class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
    <% if @owned_realties.any? %>
      <h2>Vos biens</h2>
      <% @owned_realties.each do |realty| %>
        <div class="col-md-4">
          <div class="card mb-4">
            <div class="card-body">
              <h5 class="card-name"><%= realty.name %></h5>
              <%= link_to "Votre bien", realty_path(realty), class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
    <%= link_to "Créer votre realty", new_realty_path  %>
</div>
<div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <%= image_tag("aze.webp", width: 400) %>

    </div>
    <div class="carousel-item">
      <img class="d-block w-100" src="aze.webp" alt="Second slide">
    </div>
    <div class="carousel-item">
      <img class="d-block w-100" src="aze.webp" alt="Third slide">
    </div>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
